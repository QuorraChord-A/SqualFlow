{
  "id": "verify-database-flow-status",
  "description": "验证 flow 的当前状态",
  "params": ["flowId", "expectedStatus"],
  "steps": [
    "使用 curl 查询 GET /api/flows/{flowId}"
  ],
  "verify": [
    "响应 JSON 的 status 字段等于 '{expectedStatus}'"
  ]
}
