{
  "id": "verify-database-flow-tasks",
  "description": "验证 flow_tasks 表中存在指定状态的 task 记录",
  "params": ["flowId", "expectedStatus", "minCount"],
  "steps": [
    "使用 curl 查询 GET /api/flows/{flowId}/tasks"
  ],
  "verify": [
    "响应 JSON 中 status 为 '{expectedStatus}' 的 task 数量大于等于 {minCount}"
  ]
}
