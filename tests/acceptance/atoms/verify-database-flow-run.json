{
  "id": "verify-database-flow-run",
  "description": "验证 flow_runs 表中是否存在指定 run_id 的记录",
  "params": ["flowId", "runId"],
  "steps": [
    "使用 curl 查询 GET /api/flows/{flowId}/runs"
  ],
  "verify": [
    "响应 JSON 的 runs 列表中包含 id 为 {runId} 的记录"
  ]
}
