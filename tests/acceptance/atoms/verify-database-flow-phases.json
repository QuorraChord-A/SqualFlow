{
  "id": "verify-database-flow-phases",
  "description": "验证 flow_phases 表中存在指定的 phase 记录",
  "params": ["flowId", "runId", "expectedPhases"],
  "steps": [
    "使用 curl 查询 GET /api/flows/{flowId}/runs",
    "找到 run_id 为 {runId} 的 run"
  ],
  "verify": [
    "该 run 的 phases 列表按顺序包含 {expectedPhases} 中列出的所有 phase 名称"
  ]
}
